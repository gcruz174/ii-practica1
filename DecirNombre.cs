using UnityEngine;

public class DecirNombre : MonoBehaviour
{
    // Update is called once per frame
    private void Update()
    {
        Debug.Log("Hola, soy " + gameObject.name);
    }
}
